name = "civic-pulse"
compatibility_date = "2024-03-25"
pages_build_output_dir = "dist"

# Set COUNTRY and SITE_URL in the Cloudflare Pages dashboard
# (Settings → Environment variables) rather than committing them here.
